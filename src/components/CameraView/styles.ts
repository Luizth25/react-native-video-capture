import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    justifyContent: "flex-end",
  },
  buttonContainer: {
    alignSelf: "center",
    marginBottom: 20,
  },
  buttonRecord: {
    backgroundColor: "#fff",
    padding: 8,
    borderRadius: 12,
  },
  buttonText: {
    color: "#000",
  },
});
