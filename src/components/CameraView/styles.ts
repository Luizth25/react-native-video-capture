import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
  buttonContainer: {
    alignSelf: "flex-end",
  },
  buttonRecord: {
    backgroundColor: "#fff",
    padding: 8,
    borderRadius: 4,
  },
  buttonText: {
    color: "#000",
  },
});
